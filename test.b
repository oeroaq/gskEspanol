d= 12**3
im(d)
imp(d)
imprimir(d)
max:
    funcion (a,b,c)= 12a -b -0.1a  + (d**12)dc + (d)c + (d) + d + 1
restringir:
    12a -b -0.1a > (d)
    13c - 21 >= (25**2)
con:
    a>0
    b<=(d*2)
res

max:
    fun (a,b,c)= 12a -b -0.1a  + (d**12)dc + (d)c + (d) + d + 1
restringir:
    12a -b -0.1a > (d)
    13c - 21 >= (25**2)
con:
    a>0
    b<=(d*2)
res

#esto es un comentario
##
##esto es un comentario
esto es un comentario multilinea

##